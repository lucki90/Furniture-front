/**
 * Stałe SVG dla renderowania szafek kuchennych.
 * Centralny punkt zarządzania wartościami używanymi w wizualizacji.
 */

/** Grubość standardowej płyty meblowej (mm) */
export const PLATE_THICKNESS_MM = 18;

/** Wysokość standardowego piekarnika do zabudowy (mm) */
export const OVEN_HEIGHT_STANDARD_MM = 595;

/** Wysokość kompaktowego piekarnika do zabudowy (mm) */
export const OVEN_HEIGHT_COMPACT_MM = 455;

/** Domyślna głębokość blatu kuchennego (mm) */
export const COUNTERTOP_DEPTH_DEFAULT_MM = 600;

/** Domyślna wysokość dolnego segmentu kaskady (mm) */
export const CASCADE_LOWER_HEIGHT_DEFAULT_MM = 400;

/** Domyślna głębokość górnego segmentu kaskady (mm) */
export const CASCADE_UPPER_DEPTH_DEFAULT_MM = 320;

/** Domyślna wysokość frontu zamrażarki w lodówce dwudrzwiowej (mm) */
export const FRIDGE_LOWER_FRONT_DEFAULT_MM = 713;

/** Domyślna całkowita wysokość lodówki w zabudowie (mm) */
export const FRIDGE_TOTAL_HEIGHT_DEFAULT_MM = 2000;

/** Domyślna wysokość segmentu drzwiowego w słupku kuchennym (mm) */
export const TALL_SEGMENT_DOOR_DEFAULT_MM = 600;

/** Domyślna wysokość segmentu szufladowego w słupku kuchennym (mm) */
export const TALL_SEGMENT_DRAWER_DEFAULT_MM = 400;
